using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class dkdk : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("dkdk");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
